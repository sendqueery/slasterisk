package slasterisk

//add functions to get a list of users in slack and their extensions (define attribute to scrape in config.json)
func GetSlackUserExtensions() {
	return
}

func GetSlackUsers(token string) (users *slackUserAPIResponse) {
	return
}
